const WEEK=[{v:1,s:'周一'},{v:2,s:'周二'},{v:3,s:'周三'},{v:4,s:'周四'},{v:5,s:'周五'},{v:6,s:'周六'},{v:7,s:'周日'}];
const $=id=>document.getElementById(id);let schedules=[],members=[],selected=today();
function today(){return dateStr(new Date())}function dateStr(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function parse(s){const[a,b,c]=s.split('-').map(Number);return new Date(a,b-1,c,12)}function wd(s){const n=parse(s).getDay();return n||7}function shift(s,n){const d=parse(s);d.setDate(d.getDate()+n);return dateStr(d)}function week(s){const d=parse(s);d.setDate(d.getDate()-(wd(s)-1));return WEEK.map((w,i)=>{const x=new Date(d);x.setDate(d.getDate()+i);return{...w,date:dateStr(x),md:`${x.getMonth()+1}/${x.getDate()}`}})}function kind(x){return x.kind==='temporary'?'temporary':'fixed'}function rows(date){return schedules.filter(x=>kind(x)==='fixed'?Number(x.day)===wd(date):x.date===date).sort((a,b)=>a.startTime.localeCompare(b.startTime))}function esc(v){const d=document.createElement('div');d.textContent=v??'';return d.innerHTML}function tm(t){const[h,m]=String(t).split(':').map(Number);return(h||0)*60+(m||0)}function mt(n){return `${String(Math.floor(n/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`}function slots(){const a=[];for(let m=540;m<1260;m+=30)a.push(mt(m));return a}function slotRows(date,slot){const a=tm(slot),b=a+30;return rows(date).filter(x=>tm(x.startTime)<b&&tm(x.endTime)>a)}function slotStarts(date,slot){const a=tm(slot),b=a+30;return slotRows(date,slot).filter(x=>tm(x.startTime)>=a&&tm(x.startTime)<b)}function slotContinues(date,slot){const a=tm(slot);return slotRows(date,slot).filter(x=>tm(x.startTime)<a&&tm(x.endTime)>a)}function continuesPastSlot(x,slot){return tm(x.endTime)>tm(slot)+30}
function render(){const days=week(selected);$('status').textContent=`云端共享 · ${members.length} 位会员 · 打开网页即可排课`;$('app').innerHTML=`<div class="weeknav"><button class="secondary" id="prev">‹ 上一周</button><div class="notice summary">${days[0].date} — ${days[6].date}</div><button class="secondary" id="now">本周</button><button class="secondary" id="next">下一周 ›</button></div><div class="weekdays">${days.map(d=>`<button class="day ${d.date===selected?'active':''}" data-date="${d.date}"><span>${d.s}</span><small>${d.md} · ${rows(d.date).length}节</small></button>`).join('')}</div><div class="timeline">${slots().map(s=>slot(s)).join('')}<div class="timeline-end"><span>21:00</span></div></div><div class="members"><div class="members-head"><h2>会员</h2><input id="memberListSearch" placeholder="搜索会员"></div><div id="memberList">${memberList(members)}</div></div>`;$('prev').onclick=()=>{selected=shift(selected,-7);render()};$('next').onclick=()=>{selected=shift(selected,7);render()};$('now').onclick=()=>{selected=today();render()};document.querySelectorAll('.day').forEach(b=>b.onclick=()=>{selected=b.dataset.date;render()});document.querySelectorAll('.course').forEach(b=>b.onclick=()=>openCourse(schedules.find(x=>x.id===b.dataset.id)));document.querySelectorAll('.slot-add').forEach(b=>b.onclick=()=>openCourse(null,b.dataset.time));document.querySelectorAll('.member-edit').forEach(b=>b.onclick=()=>openMember(members.find(x=>x.id===b.dataset.id)));$('memberListSearch').oninput=e=>{$('memberList').innerHTML=memberList(members.filter(m=>m.name.toLowerCase().includes(e.target.value.trim().toLowerCase())));document.querySelectorAll('.member-edit').forEach(b=>b.onclick=()=>openMember(members.find(x=>x.id===b.dataset.id)));};}
function slot(s){const starts=slotStarts(selected,s),cont=slotContinues(selected,s);let body='';if(starts.length)body+=starts.map(x=>card(x,continuesPastSlot(x,s))).join('');if(cont.length)body+=cont.map(continuationCard).join('');if(!starts.length&&!cont.length)body=`<button class="slot-add" data-time="${s}">＋ 点击约课</button>`;return `<div class="slot ${cont.length?'occupied-continuation':''}"><div class="slot-time">${s}</div><div class="slot-body">${body}</div></div>`}function card(x,continues=false){return `<button class="course ${continues?'continues':''}" data-id="${x.id}"><div><strong>${x.startTime}–${x.endTime} · ${esc(x.studentName)}</strong><small>${esc(x.type||'综合')}${x.notes?` · ${esc(x.notes)}`:''}</small></div><span class="tag ${kind(x)==='temporary'?'temp':''}">${kind(x)==='temporary'?'临时约课':'固定课'}</span></button>`}function continuationCard(x){return `<button class="course continuation" data-id="${x.id}"><div><small>占用中 · 至 ${x.endTime} · ${esc(x.studentName)}</small></div></button>`}function memberList(arr){return arr.length?arr.map(m=>`<button class="member-edit" data-id="${m.id}"><strong>${esc(m.name)}</strong><small>${esc(m.goal||'未填写目标')}${m.contact?` · ${esc(m.contact)}`:''}</small></button>`).join(''):'<div class="empty small">还没有会员，点击右上角“＋会员”创建。</div>'}
function toggleFields(){$('fixedField').style.display=$('kind').value==='fixed'?'':'none';$('tempField').style.display=$('kind').value==='temporary'?'':'none'}function fillMembers(q='',selectedName=''){const a=members.filter(m=>!q||m.name.toLowerCase().includes(q.toLowerCase()));$('studentName').innerHTML=a.map(m=>`<option value="${esc(m.name)}" ${m.name===selectedName?'selected':''}>${esc(m.name)}</option>`).join('')||'<option value="">请先创建会员</option>'}
function openCourse(x=null,preset=''){if(!members.length)return openMember();$('courseTitle').textContent=x?'编辑课程':'安排课程';$('courseId').value=x?.id||'';$('kind').value=kind(x||{});$('studentSearch').value='';fillMembers('',x?.studentName||members[0].name);$('weekday').value=x?.day||wd(selected);$('courseDate').value=x?.date||selected;$('startTime').value=x?.startTime||preset||'16:00';$('endTime').value=x?.endTime||(preset?mt(Math.min(tm(preset)+60,1260)):'17:00');$('courseType').value=x?.type||'综合';$('notes').value=x?.notes||'';$('deleteBtn').classList.toggle('hidden',!x);toggleFields();$('courseDialog').showModal()}
function openMember(x=null){$('memberTitle').textContent=x?'编辑会员':'创建会员';$('memberId').value=x?.id||'';$('memberName').value=x?.name||'';$('memberSex').value=x?.sex||'';$('memberBirth').value=x?.birth||'';$('memberGoal').value=x?.goal||'';$('memberContact').value=x?.contact||'';$('memberNotes').value=x?.notes||'';$('deleteMemberBtn').classList.toggle('hidden',!x);$('memberDialog').showModal()}
async function load(){try{[schedules,members]=await Promise.all([TrainLogCloud.listSchedules(),TrainLogCloud.listMembers()]);render()}catch(e){$('status').textContent='同步失败：'+e.message;$('app').innerHTML='<div class="empty">无法连接共享课表</div>'}}
$('kind').onchange=toggleFields;$('studentSearch').oninput=e=>fillMembers(e.target.value,$('studentName').value);$('addBtn').onclick=()=>openCourse();$('memberBtn').onclick=()=>openMember();$('cancelCourse').onclick=()=>$('courseDialog').close();$('cancelMember').onclick=()=>$('memberDialog').close();$('weekday').innerHTML=WEEK.map(x=>`<option value="${x.v}">${x.s}</option>`).join('');
$('courseForm').onsubmit=async e=>{e.preventDefault();const x={studentName:$('studentName').value,kind:$('kind').value,day:$('kind').value==='fixed'?Number($('weekday').value):null,date:$('kind').value==='temporary'?$('courseDate').value:'',startTime:$('startTime').value,endTime:$('endTime').value,type:$('courseType').value,notes:$('notes').value.trim()};if(!x.studentName)return alert('请选择会员');if(x.endTime<=x.startTime)return alert('结束时间需要晚于开始时间');if(x.kind==='temporary'&&!x.date)return alert('临时约课需要日期');try{const id=$('courseId').value;if(id)await TrainLogCloud.updateSchedule(id,x);else await TrainLogCloud.createSchedule(x);$('courseDialog').close();await load()}catch(err){alert('保存失败：'+err.message)}};
$('deleteBtn').onclick=async()=>{const id=$('courseId').value;if(!id||!confirm('确定删除这节课程？'))return;try{await TrainLogCloud.deleteSchedule(id);$('courseDialog').close();await load()}catch(err){alert('删除失败：'+err.message)}};
$('memberForm').onsubmit=async e=>{e.preventDefault();const x={name:$('memberName').value.trim(),sex:$('memberSex').value,birth:$('memberBirth').value,goal:$('memberGoal').value.trim(),contact:$('memberContact').value.trim(),notes:$('memberNotes').value.trim()};if(!x.name)return alert('请输入会员姓名');try{const id=$('memberId').value;if(id)await TrainLogCloud.updateMember(id,x);else await TrainLogCloud.createMember(x);$('memberDialog').close();await load()}catch(err){alert('会员保存失败：'+err.message)}};
$('deleteMemberBtn').onclick=async()=>{const id=$('memberId').value;if(!id||!confirm('确定删除该会员？已排课程不会自动删除。'))return;try{await TrainLogCloud.deleteMember(id);$('memberDialog').close();await load()}catch(err){alert('删除失败：'+err.message)}};load();
